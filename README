# npm init
# npm i express
# npm i nodemon
# npm i dotenv
# npm i body-parser
# npm i cors
# edit package.json 
"scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js"
  },

  docker pull mongo
docker run -p 27017:27017 --name mongo -d mongo
